# ============================================================================
# .env.example — Copy this to .env on your server and fill in real values.
# The .env file is excluded from git via .gitignore.
# ============================================================================

# MySQL credentials (used by infrastructure.yml and app-stack.yml)
DB_USER=robot_user
DB_PASSWORD=1234
DB_ROOT_PASSWORD=1234
